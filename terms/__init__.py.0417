"""
The flask application package.
"""

from flask import Blueprint, Flask
from terms.word import terms_blueprint

app = Flask(__name__)

if __name__ == "__main__":
   app.run(debug=True)

print("### terms_blueprint init ###")
# 파일 이름이 word.py이므로
app.register_blueprint(terms_blueprint)
